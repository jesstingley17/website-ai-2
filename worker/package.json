{
  "name": "backend-proxy-worker",
  "version": "1.0.0",
  "description": "Cloudflare Worker to proxy requests to Python backend",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240125.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.19.0"
  }
}

